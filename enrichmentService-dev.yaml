logging:
  level:
    root: INFO
    com.bankguard.enrichmentservice: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

external:
  alertcase-service:
    url: lb://alert-case-service
  decision-engine-service:
    url: lb://gemini_test_try2
